# 验证指南 — 手动 Web UI 测试场景

## 启动
```bash
pip install -r requirements.txt
python run.py           # 启动服务 http://localhost:8000
```

---

## 场景 1：懂我 — 记忆召回与个性化

**输入**：
```
上地片区今天掉线率怎么样？
```

**预期**：
- 🧠 召回条显示 3~5 条记忆（含用户画像、交互偏好、海淀拓扑、KPI阈值）
- 回复中使用"SDZ"站号前缀（来自地名映射记忆）
- 回复不解释"掉线率是什么"（来自交互偏好："不要解释告警定义"）
- Tool Trace 展开后可看到：ToolRouter → MemorySearch → MemoryRecall × N → LLMClient

---

## 场景 2：懂网 — 故障排查

**输入**：
```
多个基站同时出现S1链路断告警，怎么排查？
```

**预期**：
- 🧠 召回"故障模式_传输中断多站退服"记忆
- 回复给出分步排查路径（来自故障知识记忆）
- 回复提到"先按汇聚节点分组"（来自记忆中的排查流程）
- 不会从零解释"S1接口是什么"

---

## 场景 3：搜索 — 3GPP 标准查询

**输入**：
```
3GPP TS 36.331中RRC连接建立流程是什么？
```

**预期**：
- 🔍 搜索条显示搜索结果（来自模拟知识库）
- Tool Trace 中有 `SearchTool.search` 步骤
- ⏭️ 不触发记忆提取（搜索型问题跳过提取）
- 回复引用搜索结果中的 RRC 连接建立三步流程

---

## 场景 4：搜索 — 厂商文档查询

**输入**：
```
华为eNodeB的MML命令怎么查看当前告警？
```

**预期**：
- 🔍 搜索命中"华为MML命令手册"
- 回复包含 `LST ALMAF` / `DSP ALARM` 命令
- 不触发记忆提取

---

## 场景 5：安全操作 — 参数修改

**输入**：
```
帮我把SDZ-012的上行IoT告警门限从-105dBm调到-100dBm
```

**预期**：
- 🛡️ 风险评估条显示"🟡 确认执行"
- 🧠 召回权限记忆（操作类自动加载 role_permission）
- 回复包含回退命令
- Tool Trace 中有 `SafetyGuardian.risk_assess` 步骤

---

## 场景 6：安全操作 — 批量（高风险）

**输入**：
```
帮我批量修改上地片区25个站的A3偏移量
```

**预期**：
- 🛡️ 风险评估为"🟠 审批执行"
- 回复提示需要走审批流程，不直接执行

---

## 场景 7：记忆融合 — 冲突覆盖

**步骤**：
1. 先在记忆库中确认种子数据"LTE关键KPI阈值"的内容包含"预警>1.5%"
2. 输入：
```
记住：我们的掉线率预警阈值应该用2%，不是1.5%
```
3. **预期**：
   - ✨ 提取条显示提取了记忆
   - 🔀 融合条显示"⚡ 冲突覆盖"
   - 点击融合条 → 飞出面板显示旧值(1.5%) vs 新值(2%)的对比
   - 在记忆库中点击"LTE关键KPI阈值" → 详情弹窗底部有 📜 融合历史

---

## 场景 8：候选池 — 低置信度分流

**步骤**：
1. 输入：
```
我觉得CLI方式查告警比较快
```
2. **预期**：
   - 如果置信度 < 0.85，⏳ 候选条显示"1条进入候选池"
   - 点击侧边栏 ⏳ 按钮 → 候选池面板显示该条候选
   - 进度条显示 1/2（操作习惯阈值=2）
3. 再输入相似内容（不同会话也行）：
```
我习惯用命令行操作，GUI太慢了
```
4. **预期**：
   - 候选命中第2次 → ✨ 激活为正式记忆

---

## 场景 9：记忆过滤 — 不该存的不存

**输入**：
```
给我介绍一下什么是MIMO波束赋形？
```

**预期**：
- 🔍 搜索条显示搜索结果
- ⏭️ 不提取记忆（通用技术知识 + 搜索型 → 双重过滤）
- Tool Trace 中 MemoryExtractor 显示"skipped"

---

## 场景 10：记忆蒸馏

**步骤**：
1. 点击右侧面板"健康度" tab
2. 查看维度分布、新鲜度分布、类型明细
3. 点击"🌙 执行记忆蒸馏"按钮
4. **预期**：
   - Toast 通知显示蒸馏结果（清理N条、合并N条）
   - 记忆库刷新

---

## 场景 11：多会话

**步骤**：
1. 点击"＋ 开始新会话"
2. 在新会话中对话
3. 切换回旧会话 → 消息完整保留
4. 重启服务 `python run.py` → 会话依然存在

---

## 场景 12：对话导出

**步骤**：
1. 在一个有内容的会话中
2. 点击侧边栏底部 📄 按钮
3. **预期**：下载 `chat_2026-xx-xx.md` Markdown 文件

---

## 场景 13：记忆管理

**步骤**：
1. 右侧面板搜索框输入"掉线"→ 过滤显示相关记忆
2. 点击一条记忆 → 查看详情弹窗（含类型/描述/内容/Why/HowToApply/标签/来源/置信度）
3. 点击"编辑" → 修改内容 → 保存
4. 点击"+ 新增" → 创建一条新记忆

---

## 自动化验证

```bash
# Mock模式（不需要LLM服务）
python tests/test_all.py

# 真实LLM模式（需要LLM服务可用）
REAL_LLM=1 python tests/test_all.py
```
