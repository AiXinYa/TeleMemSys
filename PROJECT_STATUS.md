# 电信运维记忆系统 · 项目进度 v1.0.1

## 概况
| 指标 | 值 |
|------|-----|
| Python | 3950+ 行 (16模块) |
| 前端 | 176 行 React SPA |
| 测试 | 74/74 通过 |
| API | 25 端点 |
| LLM | Qwen3-32B (可配置) |

## ✅ 已完成

### 记忆核心
- 10种记忆类型 (懂我4/懂角色2/懂网4) + merge_history
- 文件存储 + JSON索引 + jieba搜索 + 同义词扩展30+组
- L1索引注入 + L2召回(常驻user_profile/interaction_pref/operation_habit)
- 11条种子记忆

### 提取+融合+过滤
- LLM提取: 只从用户发言提取，剥离`<think>`标签，排除助手回复/思考过程中的已有记忆回显
- 三级管道: 提取→过滤→候选池(分级阈值)/直接激活→融合
- 融合引擎: 匹配→LLM判断关系→可信度裁决冲突(user_explicit 1.0>seed 0.5)
- 去重: _is_already_known(内容>70%重叠) + _is_content_same(>85%=完全相同)
- 过滤: 通用知识/标准/厂商/法规不存; 搜索结果不存
- 指令性内容("记住/以后/别再")即使搜索型也强制提取
- 澄清回答自动提取(confidence=0.85)

### 工具路由+搜索
- ToolRouter按信号词分流(记忆/搜索/安全)
- MockKB 10条电信知识 + Tavily/SerpAPI预留
- /memorydream 和 /dream 命令直接执行蒸馏

### 安全防护
- 四级评估(绿/黄/橙/红) + 前置检查 + 回退方案

### 对话引擎
- 多会话SQLite持久化 + SSE流式(前端剥离think标签显示)
- Tool Trace默认展开(带步骤编号#1~#N)
- 追问选择题(支持JSON格式+ABC文本格式自动解析, 前端渲染为可点击按钮)
- 融合后双次刷新(500ms+2000ms)

### 前端
- 可拖拽三栏布局(sidebar 180~420, 右面板 260~650)
- 15个Lucide SVG图标
- 5个面板tab: 记忆库(搜索+维度分组)/候选池(进度条)/工具(7阶段)/健康度/导入导出
- 追问按钮(ClarQ组件, 点击直接send) + 融合飞出面板 + merge_history时间线
- 对话导出Markdown

## 📋 待完成
| 优先级 | 项目 | 预估 |
|--------|------|------|
| P1 | 向量检索Chroma | 2-3天 |
| P1 | 外部系统MCP对接 | 3-5天 |
| P1 | 操作执行模拟 | 3天 |
| P1 | 认证多用户 | 2-3天 |
| P2 | 响应式适配 | 1天 |
| P2 | 搜索引用标注 | 1天 |
| P3 | 预测性记忆/学习进度/多LLM | 长期 |

## 关键修复历史
| 问题 | 根因 | 修复 |
|------|------|------|
| 种子数据赢冲突 | confidence默认1.0 | 改为0.0,按source查表 |
| 召回→再提取循环 | 助手回复含已有记忆 | 分离用户/助手标签+已有记忆摘要+_is_already_known |
| 冲突不覆盖 | is_explicit跳过去重 | 改为只跳过内容不同的更新(真更新) |
| think过程被提取 | 提取器收到含think的文本 | re.sub剥离+前端streaming剥离+prompt强化 |
| 首轮不提取 | 阈值>=4 | 降为>=2 |
