# 电信运维记忆系统 — 详细流程说明

## 一、整体架构：一次对话的完整处理流程

```
用户输入
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step 1: ToolRouter 工具路由                      │
│ 分析查询中的信号词，决定本次使用哪些工具            │
│ 输出: use_memory / use_search / use_safety       │
│       skip_memory_extract / search_category       │
└───────┬─────────────┬──────────────┬─────────────┘
        │             │              │
        ▼             ▼              ▼
┌──────────────┐ ┌──────────┐ ┌──────────────┐
│ Step 2:      │ │ Step 3:  │ │ Step 4:      │
│ 记忆召回     │ │ 知识搜索 │ │ 安全评估     │
│ L1索引+L2搜索│ │ MockKB/  │ │ 绿/黄/橙/红  │
│ (同义词扩展) │ │ WebAPI   │ │              │
└──────┬───────┘ └────┬─────┘ └──────┬───────┘
       │              │              │
       ▼              ▼              ▼
┌─────────────────────────────────────────────────┐
│ Step 5: PromptBuilder 构建 System Prompt         │
│ = L0固定规则 + L1记忆索引 + L2召回记忆全文         │
│   + 搜索结果 + 安全提醒 + 追问规则                 │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│ Step 6: LLMClient 调用大模型                      │
│ 输入: System Prompt + 历史对话(≤20条) + 当前问题   │
│ 输出: reply（已剥离<think>标签）                   │
└───────────────────┬─────────────────────────────┘
                    │
                    ├──→ 解析追问选择题 (ClarificationParser)
                    │    支持 <!--CLARIFICATION-->JSON 和 A/B/C 文本格式
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│ Step 7: 持久化消息                                │
│ user消息 + assistant消息 → SQLite (session_store) │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│ Step 8: 后台记忆提取 + 融合 (异步线程)             │
│                                                   │
│ 8a. 格式化对话 → 只标注用户消息为提取来源           │
│     助手消息剥离<think>后仅作上下文参考             │
│                                                   │
│ 8b. LLM 提取 → 返回候选知识列表                   │
│                                                   │
│ 8c. 逐条过滤:                                     │
│     · 代码过滤(通用知识/标准/厂商不存)              │
│     · 已有记忆去重(_is_already_known)              │
│     · 内容相同拦截(_is_content_same)               │
│                                                   │
│ 8d. 分流:                                         │
│     · confidence≥0.85 或 is_user_explicit → 直接激活│
│     · 0.4≤conf<0.85 → 进候选池(按类型阈值累积)     │
│     · conf<0.4 → 丢弃                             │
│                                                   │
│ 8e. 融合(每条激活的记忆都经过):                     │
│     · _match: 匹配同类型已有记忆(名称+标签+内容)    │
│     · _judge: LLM判断关系(补充/冲突/替代/重复)      │
│     · _execute: 按可信度裁决 → 写入/更新/跳过       │
└─────────────────────────────────────────────────┘
```

## 二、记忆召回：Agent 回复时如何使用记忆

### 召回时机
每次用户发消息时（除了 /dream 命令），如果 ToolRouter 判断 `use_memory=True`（几乎所有非纯搜索查询都是 True），就会执行记忆召回。

### 召回流程

```
用户查询: "上地片区掉线率怎么样"
    │
    ▼
┌─ L1: 索引加载 ─────────────────────────────┐
│ 读取 MEMORY_INDEX.json（≤200行摘要）          │
│ 全文注入 System Prompt 的 {memory_index}      │
│ 让 LLM 知道"系统里有哪些记忆可用"             │
└─────────────────────────────────────────────┘
    │
    ▼
┌─ L2: 主动召回（≤5条）──────────────────────┐
│                                             │
│ ① 常驻记忆（每次都加载）:                    │
│    · user_profile（用户画像）                 │
│    · interaction_pref（交互偏好）             │
│    · operation_habit（操作习惯）              │
│                                             │
│ ② 操作感知加载（仅操作类查询触发）:           │
│    · role_permission（权限规则）              │
│    · change_state（近期变更）                 │
│                                             │
│ ③ 关键词搜索（同义词扩展）:                   │
│    "上地" → 扩展为 "上地 SDZ 西二旗 码农集中营"│
│    "掉线" → 扩展为 "掉线率 掉话 RRC释放 drop" │
│    在所有记忆中搜索，按 jieba分词 + 类型加权   │
│    取 top 5                                  │
│                                             │
│ 输出: 每条记忆的全文（name/content/why/       │
│       how_to_apply/tags/freshness）           │
└─────────────────────────────────────────────┘
    │
    ▼
┌─ 注入 System Prompt ───────────────────────┐
│ 召回的记忆全文注入 {recalled_memories} 占位符  │
│                                             │
│ 格式:                                       │
│ ## 记忆 1: 张工_用户画像 [用户画像]           │
│ 新鲜度: 🟢 今天                              │
│ 身份：张工，高级运维工程师...                  │
│ **Why:** 了解用户身份以调整回答深度            │
│ **How to apply:** 使用专家级术语...           │
│                                             │
│ ⚠️ 记忆是历史观察，涉及网元/配置先验证当前值   │
└─────────────────────────────────────────────┘
```

### 同义词表（30+组）
```
"掉线" ↔ 掉线率/掉话/RRC释放/异常释放/drop
"上地" ↔ SDZ/西二旗/码农集中营
"中关村" ↔ ZGC/村里
"干扰" ↔ 底噪/IoT/上行干扰/SINR
"切换" ↔ handover/HO/切换成功率
"基站" ↔ eNodeB/gNodeB/站点/网元
...
```

## 三、记忆提取：什么时候提取、输入是什么

### 提取触发条件

```python
# 触发条件（满足任一）:
_should_extract = True 当:
  1. routing.skip_memory_extract == False  # 非搜索型问题
  2. 用户消息包含指令词: "记住/以后/别再/我要求/我是/我负责/不要/我喜欢/我习惯"
     （即使是搜索型问题也强制提取）

# 额外条件:
  3. 会话中至少有2条消息（1条用户+1条助手）
```

### 提取的输入

```
输入给提取LLM的内容:
┌────────────────────────────────────────┐
│ System Prompt = 提取规则 + 已有记忆摘要  │
│                                        │
│ User Prompt = 格式化的对话（最近10条）:   │
│                                        │
│ [用户（提取来源）]: 我现在负责朝阳区了    │
│ [助手回复（仅参考上下文）]: 好的，已记录…│
│ [用户（提取来源）]: 帮我查一下掉线率      │
│ [助手回复（仅参考上下文）]: 朝阳区掉线…  │
│                                        │
│ 注意:                                   │
│ · 助手消息截断到200字                    │
│ · 助手消息的<think>标签被剥离            │
│ · 已有记忆摘要注入prompt防止重复提取      │
└────────────────────────────────────────┘
```

### 提取后的三级过滤管道

```
LLM 提取结果 [{name, type, content, confidence, is_user_explicit, user_said}, ...]
    │
    ├─ 过滤1: ToolRouter.should_store()
    │  拦截: 通用知识/3GPP标准/厂商文档/法规/搜索结果
    │
    ├─ 过滤2: _is_already_known()
    │  拦截: 与已有记忆内容重叠>70% 或 (名称>70% + 内容>50%)
    │  例外: is_user_explicit=True 且内容确实不同(真更新)才放行
    │
    ├─ 过滤3: confidence < 0.4 → 丢弃
    │
    ├─ 过滤4: 无user_said且非explicit → 置信度降到0.5
    │
    ▼
┌─ 分流 ──────────────────────────────┐
│                                     │
│ confidence ≥ 0.85                   │
│ 或 is_user_explicit = True   ──→ 直接激活 → 进入融合
│ 或 type = session_context/change_state│
│                                     │
│ 0.4 ≤ confidence < 0.85      ──→ 进候选池
│                                     │
│ confidence < 0.4              ──→ 丢弃
└─────────────────────────────────────┘
```

## 四、记忆类型与存储

### 10种记忆类型

| 维度 | 类型 | 说明 | 作用域 | 保留期 | 候选阈值 |
|------|------|------|--------|--------|----------|
| **懂我** | user_profile | 身份/职责/技术水平/负责区域 | 私有 | 永久 | 2次 |
| **懂我** | interaction_pref | 对助手行为的纠正确认 | 私有 | 永久 | 2次 |
| **懂我** | operation_habit | 操作方式偏好/常用命令 | 私有 | 永久 | 2次 |
| **懂我** | session_context | 正在处理的事项/未完成任务 | 私有 | 7天 | 1次(即时) |
| **懂角色** | role_permission | 权限规则/审批流程 | 团队 | 永久 | 3次 |
| **懂角色** | external_reference | 外部系统/文档位置 | 团队 | 永久 | 2次 |
| **懂网** | network_model | 网络拓扑/网元归属/地名映射 | 团队 | 永久 | 3次 |
| **懂网** | performance_baseline | KPI阈值/参数基线 | 团队 | 30天 | 3次 |
| **懂网** | incident_knowledge | 故障模式/排查经验 | 团队 | 永久 | 2次 |
| **懂网** | change_state | 网络变更记录 | 团队 | 30天 | 1次(即时) |

### MemoryEntry 数据结构

```json
{
  "id": "mem_a3f35bfdb0c2",
  "name": "张工_用户画像",
  "description": "张工的身份和职责信息",
  "type": "user_profile",
  "scope": "private",
  "content": "身份：张工，高级运维工程师\n负责区域：北京朝阳区\n负责网元：eNodeB(BJ-CY-*)",
  "tags": ["张工", "朝阳", "LTE"],
  "why": "了解用户身份以调整回答深度",
  "how_to_apply": "回复时使用专家级术语，默认查询朝阳区数据",
  "source": "user_explicit",
  "confidence": 0.95,
  "user_id": "default",
  "created_at": "2026-05-23T10:00:00",
  "updated_at": "2026-05-23T15:30:00",
  "merge_history": [
    {
      "timestamp": "2026-05-23T15:30:00",
      "action": "conflict_override",
      "detail": "负责区域从海淀变为朝阳 | 新(0.95/user_explicit)≥旧(0.5/seed)→覆盖",
      "old_content": "负责区域：北京海淀区",
      "new_content": "负责区域：北京朝阳区",
      "conflict_fields": ["负责区域"]
    }
  ]
}
```

### 文件存储结构

```
data/
├── memories/
│   ├── MEMORY_INDEX.json        ← 全局索引（≤200行摘要）
│   ├── me/                      ← 私有记忆（private scope）
│   │   ├── mem_abc123.json      ← user_profile
│   │   ├── mem_def456.json      ← interaction_pref
│   │   └── ...
│   └── team/                    ← 团队记忆（team scope）
│       ├── network/             ← network_model
│       ├── baselines/           ← performance_baseline
│       ├── incidents/           ← incident_knowledge
│       ├── changes/             ← change_state
│       └── roles/               ← role_permission
├── sessions.db                  ← SQLite（sessions + messages + turn_meta）
└── candidate_pool.json          ← 候选记忆池
```

### MEMORY_INDEX.json 结构

```json
[
  {
    "id": "mem_a3f35bfdb0c2",
    "name": "张工_用户画像",
    "type": "user_profile",
    "description": "张工的身份和职责信息",
    "file_path": "me/mem_a3f35bfdb0c2.json",
    "updated_at": "2026-05-23T15:30:00"
  },
  ...
]
```

## 五、融合流程：新知识如何与已有记忆合并

```
新提取的知识项
    │
    ▼
┌─ Match: 匹配已有记忆 ──────────────────┐
│ 在同类型记忆中计算三维相似度:             │
│ · 名称 Levenshtein 相似度 × 0.4         │
│ · 标签重叠度 × 0.3                       │
│ · 内容关键词(jieba)重叠度 × 0.3          │
│ 取 score > 0.3 的前3个候选              │
│                                         │
│ 无匹配 → 🆕 新增（直接save）             │
└────────┬────────────────────────────────┘
         │ 有匹配
         ▼
┌─ Judge: 判断关系 ──────────────────────┐
│ 优先用 LLM 判断（规则降级兜底）:         │
│ · 补充: 新信息是已有记忆的细化/扩展      │
│ · 冲突: 同一事实给出不同值              │
│ · 替代: 新内容完全取代旧内容            │
│ · 重复: 语义完全相同                    │
│ · 无关: 虽同类型但描述不同事物           │
└────────┬────────────────────────────────┘
         │
         ▼
┌─ Execute: 按可信度裁决 ────────────────┐
│                                        │
│ 可信度等级:                             │
│ user_explicit = 1.0  (用户明确说的)     │
│ manual       = 0.9  (界面手动创建)      │
│ extracted    = 0.7  (自动提取)          │
│ skill_bridge = 0.65 (技能对接)          │
│ imported     = 0.6  (批量导入)          │
│ seed         = 0.5  (种子数据)          │
│                                        │
│ 冲突时:                                │
│ · 新conf ≥ 旧conf → ⚡覆盖旧值          │
│ · 新conf < 旧conf → 🛡️保留旧值          │
│                                        │
│ 补充 → 📝追加内容                       │
│ 替代 → 🔄完全替换                       │
│ 重复 → ⏭️跳过(仅合并标签)               │
│                                        │
│ 每次操作写入 merge_history 审计轨迹      │
└────────────────────────────────────────┘
```

## 六、特殊流程

### 追问选择题
```
用户: "北区的网络怎么样"
    │
    ▼ LLM回复包含选择题（A/B/C 或 JSON格式）
    │
    ▼ _parse_clarification() 自动解析
    │  支持: <!--CLARIFICATION-->JSON / A.B.C.文本 / 1.2.3.文本
    │
    ▼ 前端渲染为可点击的蓝色按钮
    │
    ▼ 用户点击 "北京北部区域"
    │
    ▼ send("北京北部区域") → 作为新用户消息发送
    │
    ▼ 下一轮对话正常处理 → 这条澄清回答会被提取为记忆
```

### /dream 蒸馏命令
```
用户: "/dream"
    │
    ▼ ChatEngine 检测到命令
    │
    ▼ MemoryDream.run()
    │  Phase 1: 清理过期记忆（按retention_days）
    │  Phase 2: 合并同类型重复（LLM辅助 + 简单策略降级）
    │  Phase 3: 修剪MEMORY_INDEX（≤200行）
    │
    ▼ 返回蒸馏报告（不调用LLM对话）
```
